# Humboldti Note

ターミナルで動く、ミニマルな日次マークダウンメモツール。macOS を主対象とし、
Linux でも動くこと。Rust 製。

このファイルには「守るべきこと」だけを書く。理由は `docs/DESIGN.md` にある。
下記の決定を変更する提案をする前に、必ずそちらを読むこと。

## 現状

v1 前。まだ何も公開していない。ディスク上のノート形式を除いて、後方互換の
義務はない。ノート形式だけは既に凍結済みとして扱う。

## 変更してはいけない決定

以下を変える必要が生じたら、実装せずに先に相談すること。

- **ノートはプレーンなマークダウンファイル。** `~/notes/YYYY/MM/YYYY-MM-DD.md`
  で1日1ファイル。データベースを正とはしない。
- **インデックスは再構築可能なキャッシュ。** ノートディレクトリの外に置く。
  削除してもデータが失われないこと。
- **v1 ではエディタを内蔵しない。** 引数なしの `pen` は外部エディタ(設定の
  `editor`、無ければ `$EDITOR`、それも無ければ `vi`)を起動する。
- **設定ファイルは無くても動く。** 既定値は `impl Default for Config` の
  1箇所だけに書く。他の場所に `unwrap_or(...)` で既定値を書かない。
- **キー入力は `Action` enum を経由する。** 描画コードや更新コードの中で
  `KeyEvent` を直接 match しない。
- **アクション名は公開 API。** 一度命名したら（`next_day`、`open` など）
  改名しない。利用者の設定ファイルが参照する。

## 表示幅の規則

このプロジェクトで最も頻出するバグの原因。

**`.len()` や `.chars().count()` を表示幅として使わないこと。** 日本語、絵文字、
結合文字のすべてで破綻する。`unicode-width` をラップした `src/width.rs` の
ヘルパーを使う。

文字を配置する、罫線を引く、切り詰める、折り返す — これらに触れるコードは
すべて幅ヘルパーを通すこと。レイアウトに関わる作業の前に
`.claude/skills/east-asian-width/SKILL.md` を読むこと。

## ディレクトリ

`docs/STRUCTURE.md` に全体像がある。要点だけ:

- `src/` `tests/` — Rust のコード
- `README.md` `LICENSE-*` — 利用者が受け取るもの
- `docs/` `CLAUDE.md` `.claude/` — 開発支援。git にはコミットするが
  crates.io には含めない（`Cargo.toml` の `exclude` で除外済み）

## コマンド

```sh
cargo run -- <args>
cargo test
cargo clippy -- -D warnings
cargo fmt
```

作業完了を宣言する前に `cargo clippy` と `cargo fmt` を実行すること。

## 規約

- エラーは、バイナリ境界で `anyhow`、モジュール内部で `thiserror`。
  テスト以外で `unwrap()` を使わない。
- ノートへの書き込みは必ずファイルロック（`std::fs::File::lock`）を取る。2つのターミナルから
  同時に `pen <text>` を実行してもファイルが壊れないこと。
- ファイルシステムに触れるテストは `tempfile` を使う。`~/notes` を絶対に使わない。
- **利用者に見える文字列は英語**（ヘルプ、エラーメッセージ、README）。
  国際的に公開するプロジェクトのため。コメントとドキュメントは日本語でよい。
- コメントには「何を」ではなく「なぜ」を書く。

## 進め方

- 小さく、レビューしやすい単位で変更する。1コミット1関心事。
- 妥当な選択肢が複数ある判断は、自分で決めずに聞くこと。
- 上記の「変更してはいけない決定」に触れる変更が必要になったら、実装を止めて
  その旨を伝えること。
