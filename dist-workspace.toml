[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
# Windows is intentionally excluded -- CLAUDE.md scopes this project to macOS/Linux.
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Where to host releases
hosting = "github"
# Whether to install an updater program
install-updater = false
# GitHub repo to push the generated Homebrew formula to.
# This repo does not exist yet -- create it (empty is fine, dist creates
# Formula/humboldti-note.rb on first release) before the release workflow
# can succeed, and add a `HOMEBREW_TAP_TOKEN` secret (a PAT with repo scope
# for that tap repo) to this repo's Actions secrets.
tap = "sphenisciformes-lab/homebrew-humboldti"
publish-jobs = ["homebrew"]
